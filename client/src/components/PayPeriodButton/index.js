import React from 'react';
import Button from 'react-bootstrap/Button';

export default function PayPeriodButton() {
    return (
        <div>
  <Button variant="primary" size="lg" block>
    View Pay Period
  </Button>
</div>
    )
}

